---
title: React Hook
description: react Hook
date: 2026-07-02
tags: [React]
category: frontend/react
order: 3
---
# React Hook
> Hook 是 React 16.8 的新增特性。它可以让你在不编写 class 的情况下使用 state 以及其他的 React 特性。
## Hook 规则
只在最顶层使用 Hook,
不要在循环，条件或嵌套函数中调用 Hook， 确保总是在你的 React 函数的最顶层调用他们。

原因：我们可以在单个组件中使用多个 State Hook 或 Effect Hook， React 靠的是 Hook 调用的顺序确定哪个 state 对应哪个 useState
```jsx
// ------------
// 首次渲染
// ------------
useState('Mary')           // 1. 使用 'Mary' 初始化变量名为 name 的 state
useEffect(persistForm)     // 2. 添加 effect 以保存 form 操作
useState('Poppins')        // 3. 使用 'Poppins' 初始化变量名为 surname 的 state
useEffect(updateTitle)     // 4. 添加 effect 以更新标题

// -------------
// 二次渲染
// -------------
useState('Mary')           // 1. 读取变量名为 name 的 state（参数被忽略）
useEffect(persistForm)     // 2. 替换保存 form 的 effect
useState('Poppins')        // 3. 读取变量名为 surname 的 state（参数被忽略）
useEffect(updateTitle)     // 4. 替换更新标题的 effect

//只要 Hook 的调用顺序在多次渲染之间保持一致，React 就能正确地将内部 state 和对应的 Hook 进行关联。
// 🔴 在条件语句中使用 Hook 违反第一条规则
if (name !== '') {
  useEffect(function persistForm() {
    localStorage.setItem('formData', name);
  });
}
//在第一次渲染中 name !== '' 这个条件值为 true，所以我们会执行这个 Hook。但是下一次渲染时我们可能清空了表单，表达式值变为 false。此时的渲染会跳过该 Hook，Hook 的调用顺序发生了改变：

useState('Mary')           // 1. 读取变量名为 name 的 state（参数被忽略）
// useEffect(persistForm)  // 🔴 此 Hook 被忽略！
useState('Poppins')        // 🔴 2 （之前为 3）。读取变量名为 surname 的 state 失败
useEffect(updateTitle)     // 🔴 3 （之前为 4）。替换更新标题的 effect 失败
```
只在 React 函数中调用 Hook,不要在普通的 JavaScript 函数中调用 Hook。

你可以：
- 在 React 的函数组件中调用 Hook
- 在自定义 Hook 中调用其他 Hook (我们将会在下一页 中学习这个。)
## useState 
useState 是允许你在 React 函数组件中添加 state 的 Hook 。
```jsx
// 声明一个叫 “count” 的 state 变量
const [count, setCount] = useState(0);
可以通过调用 setCount 来更新当前的 count
setCount(1)//直接复制为1
setCount((preCount)=>preCount+1)//每次执行count+1
```
原理：每个函数组件对应一个 React 节点，每个节点保存着 x 和 index ，useX 会读取 x[index] ，index 由 useState 出现的顺序决定，setX 会改变 state 的值并触发页面更新。
```javaScript
let x = [];
let index = 0;
const myUseState = initial => {
    let currentIndex = index;
    x[currentIndex] = x[currentIndex] === undefined ? initial : x[currentIndex];
     const setInitial = value=>{
      x[currentIndex] = value;
      render();
    }
    index += 1;
    return [x[currentIndex],setInitial]
}
//模拟的render函数
const render = () => {
  index = 0;  //将 index 重置
  ReactDOM.render(<App/>,document.querySelector('#root'))
}
const App = () => {
  const [n, setN] = myUseState(0)
  const [m, setM] = myUseState(0);
  return (
     <div>
         <p>n：{n}</p>
         <button onClick={()=>setN(n+1)}>+1</button>
         <p>m：{m}</p>
         <button onClick={()=>setM(m+1)}>+1</button>
     </div>
  )
}
```
## useEffect
Effect Hook 可以让在函数组件中执行副作用操作。可以把 useEffect Hook 看做 componentDidMount，componentDidUpdate 和 componentWillUnmount 这三个函数的组合。

useEffect 中可以获取真实dom

useEffect有两个参数:
- 第一个参数：是一个回调函数，用于执行操作逻辑。
- 第二个参数：
  - 不传第二个参数，监听所有数据的变化，页面加载就会执行
  - 如果是一个空数组[]执行一次，相当于componentDidMount
  - 如果数组中有数据count时，执行多次，count改变就会执行

useEffect如何有返回值，return，组件卸载时执行。相当于componentWillUnmount
React 会在组件卸载的时候执行清除操作。effect 在每次渲染的时候都会执行。React 会在执行当前 effect 之前对上一个 effect 进行清除。
```javascript
import React,{useState,useEffect} from "react"
const Hookuseffect=()=>{
    const [count,setCount]=useState(0)
    const add=()=>{
        setCount(count+1)
    }
    useEffect(()=>{
        console.log("effect执行了");
        return ()=>{console.log("先执行了销毁")}
    },[count])
    return (
        <div>
            hook
            <button onClick={add}>hook点击</button>
        </div>
    )
}
export default Hookuseffect
// 先执行了销毁
// effect执行了
```
## useLayoutEffect
useLayoutEffect用法与useEffect相同。

不同：
1. useEffect会在渲染的内容更新到DOM上后执行,不会阻塞DOM的更新
2. useLayoutEffect会在渲染的内容更新到DOM上之前进行,会阻塞DOM的更新
```javaScript
import React, { useState, useEffect } from 'react'

export default function UseLayoutEffect() {
  
  const [count,setCount] = useState(10)
  
  useEffect(()=>{
    if(count ===0){
      setCount(Math.random())
    }
  },[count])
  return (
    <div>
      <h2>数字:{count}</h2>
      <button onClick={e=>setCount(0)}>修改数字</button>
    </div>
  )
}
```
count变成一个随机数，可以实现，但是当我们点击修改的时候其实是有一个闪烁的 因为它先把数字变为了0，再变成了随机数。但是当我们这样写把里面的useEffect换成了useLayoutEffect，页面就不会发生这个闪烁了。
## useRef
useRef 返回一个可变的 ref 对象，其 .current 属性被初始化为传入的参数。
### 获取元素
```javaScript
import { useRef } from "react"
export default function Useref(){
  const reftex=useRef(null)
  function add(){
    console.log("ref",reftex.current);
  }
  return (
    <div>
        USEREF:
        <button onClick={add}>点击</button>
        <p ref={reftex}>我是P标签</p>
    </div>
  )
}
```
### 获取组件
1. 获取类组件，同上
2. 获取函数组件。ref不能直接绑定函数组件。

直接绑定函数组件会有警告，函数组件可以使用forwardRef("组件名")进行包裹
```javaScript
import React,{forwardRef}from "react"
function Ceshiref(){
  return(
    <div>
      测试ref
    </div>
  )
}
export default React.forwardRef(Ceshiref)

// Warning: Function components cannot be given refs.Attempts to access this ref will fail. Did you mean to use React.forwardRef()?
```
使用forwardRef("组件名")进行包裹进行导出后不会报错但是current为null。

如果ref需要获取到子组件的内容，需要结合useImperativeHandle进行使用

## useImperativeHandle
useImperativeHandle可以让你在使用 ref 时自定义暴露给父组件的实例值。useImperativeHandle 应当与 forwardRef 一起使。

useImperativeHandle有两个参数，一个父组件传的ref，第二个是一个回调函数，回调函数返回一个对象，子组件需要给父组件使用的内容。
```javascript
//子组件
import React,{forwardRef,useState,useImperativeHandle}from "react"
function Ceshiref(props,ref){
  const [count,setCount]=useState(0)
  useImperativeHandle(ref,()=>{
    return {
      tex:"我是返回的值",
      addcount(){
        setCount(preCount=>preCount+1)
      }
    }
  })
  return(
    <div>
      测试ref:{count}
    </div>
  )
}
export default forwardRef(Ceshiref)
//父组件
import { useRef } from "react"
import Ceshiref from "./ceshiref";
export default function Useref(){
  const ceshiref=useRef(null)
  function add(){
    console.log("ceshiref",ceshiref);
    ceshiref.current.addcount()//子组件提供的事件
  }
  return (
    <div>
      USEREF:
      <button onClick={add}>点击</button>
      <Ceshiref ref={ceshiref}/>
    </div>
)
}
```
## useContext
useContext上下文
```javascript
// 1、创建一个context
import {createContext}from 'react'
let arr=[{
  name:"张三",
  age:18
}]
const myContex=createContext(arr)
export {myContex}

// 2、父组件使用Provider传递数据
import React,{useState} from "react";
import Effect_text from "./component/hooks/effect";
import { myContex } from "./component/usecontext";
function App() {
  const [name1,setName1]=useState([{
    name:"哈利波特",
    age :20
  }])
  return (
    <div className="App">
      <myContex.Provider value={name1}>
        <Effect_text/>
      </myContex.Provider>
    </div>
  );
}
export default App;

//3、子组件使用useContext接收值
import { myContex } from "../usecontext"
import { useContext ,useState} from "react"
function Effect_text (){
  const arr=useContext(myContex)
  console.log("myContex",arr);
  return (
    <div>
      Effect:{arr[0].name}
    </div>
  )
}
export default Effect_text;

```
## useMemo
把“创建”函数和依赖项数组作为参数传入 useMemo，它仅会在某个依赖项改变时才重新计算。有助于避免在每次渲染时都进行高开销的计算。如果没有提供依赖项数组，useMemo 在每次渲染时都会计算新的值。返回一个值。
1. 不传数组，每次更新都会重新计算
2. 空数组，只会计算一次
3. 依赖对应的值，当对应的值发生变化时，才会重新计算(可以依赖另外一个 useMemo 返回的值)
```javaScript
//不使用useMemo
import { useMemo,useState } from "react"
//使用场景：有两个state，其中一个state更新，会导致整个函数都会渲染。
function Usememo(){
  const [count,setCount]=useState(0)
  const [list,setList]=useState([
    {
      name:"张三",
      id:1
    },
    {
      name:"李四",
      id:2
    }
  ])
  function list1(){//每次点击count加一，这一个函数就会执行
    console.log("执行");
    return list.map(item=><li key={item.id}>{item.name}</li>
  )
}
function add(){
  setCount(preCount=>preCount+1)
}
return (
    <div>
      <button onClick={add}>点击</button>
      Usememo:{count}
      <ul>
        {list1()}
      </ul>
    </div>
  )
}
export default Usememo

//使用useMemo
import { useMemo,useState } from "react"
//使用场景：有两个state，其中一个state更新，会导致整个函数都会渲染。
function Usememo(){
  const [count,setCount]=useState(0)
  const [list,setList]=useState([
    {
      name:"张三",
      id:1
    },
    {
      name:"李四",
      id:2
    }
  ])
  function list1(){
    console.log("执行");
    return list.map(item=><li key={item.id}>{item.name}</li>
  )
}
const Memolist1= useMemo(()=>list1(),[list])
function add(){
  setCount(preCount=>preCount+1)
}
return (
  <div>
    <button onClick={add}>点击</button>
     Usememo:{count}
    <ul>
      {Memolist1}
    </ul>
  </div>
 )
}
export default Usememo
```
## useCallback
useCallback 用来返回一个函数。避免函数在不必要的情况下更新。记得子组件导出时使用memo包裹一下，其作用是对组件前后两次进行浅对比，阻止不必要的更新。
```javascript
//不使用useCallback
//父组件
import { useCallback ,useState} from "react";
import Callceshi from "./callzi";
export default function Usecall(){
  const [count,setCount]=useState(0)
  function add(){
    setCount(preCount=>preCount+1)
  }
  function add1(){
    console.log("触发");
  }
  return (
      <div>
        <button onClick={add}>点击</button>
        useCallback:{count}
        <Callceshi add1={add1}/>
      </div>
    )
}
//每次点击count+1后子组件都会调用回调函数打印执行
//子组件
function Callceshi(props){
  return(
    <div>
      Callceshi
      {props.add1()}
    </div>
  )
}
export default Callceshi

// 使用useCallback
import { useCallback ,useState} from "react";
import Callceshi from "./callzi";
export default function Usecall(){
  const [count,setCount]=useState(0)
  function add(){
    setCount(preCount=>preCount+1)
  }
  /* function add1(){
  console.log("触发");
  } */
  const add1=  useCallback(()=>{//代替之前的add1
    console.log("触发");
  },[])
  return (
    <div>
      <button onClick={add}>点击</button>
      useCallback:{count}
      <Callceshi add1={add1}/>
    </div>
  )
}
import React from "react"
function Callceshi(props){
  return(
    <div>
      Callceshi
      {props.add1()}
    </div>
  )
}
export default React.memo(Callceshi)
 //React.memo不要忘记。
```
## useDebugValue
useDebugValue(value)可用于在 React 开发者工具中显示自定义 hook 的标签
```javascript
function useFriendStatus(friendID) {
  const [isOnline, setIsOnline] = useState(null);
  
  // 在开发者工具中的这个 Hook 旁边显示标签
  useDebugValue(isOnline ? 'Online' : 'Offline');
  
  return isOnline;
}
```
## useReducer
在 hooks 中提供了的 useReducer 功能，提供类似 Redux 的功能，引入 useReducer 后，useReducer 接受一个 reducer 函数作为参数，reducer 接受两个参数一个是 state 另一个是 action 。然后返回一个状态 state和 dispath，state是返回状态中的值，而 dispatch 是一个可以发布事件来更新 state 的。

tab.js

```javaScript 
import { useReducer } from "react"
import stateReducer from "./reducer"
function Tab(props){
  const innet={
    count:0
  }
  const[state,dispatch]=useReducer(stateReducer,innet)//stateReducer是创建的reduce。innet是默认值
  //state就是返回的新的state。通过dispatch发送action
  function add(){
    dispatch({type:'add'})
  }
  function sub(){
    dispatch({type:'sub'})
  }
  const{count}=state
  return(
    <div>
      TAB:{count}
      <br/>
      <button onClick={add}>+</button>
      <button onClick={sub}>-</button>
    </div>
  )
}
export default Tab
```
reducer.js  处理数据返回新的state
```javascript
function stateReducer (state, action) { // reducer处理函数
  console.log(state, action);
  switch (action.type) {
    case 'add':
      return{
        count:state.count + 1
      } 
    case 'sub':
      return {
        count:state.count -1
      } 
    default:
      return state;
  }
}
 export default stateReducer
 ```