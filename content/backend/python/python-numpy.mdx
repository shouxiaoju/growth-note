---
title: NumPy 教程
description:  NumPy 是python语言的一个扩展程序库，支持大量的维度数组与矩阵运算。
date: 2026-09-23
tags: [python]
category: backend/python
order: 3
---

# NumPy 简介
> NumPy是python语言的一个扩展程序库，主要用于处理大模型多维数组和矩阵，以及提高大量的数学函数来操作这些数据结构。
核心：提供高性能的多维数组对象ndarry。
优势：底层用C语言实现，比python原生列表快10-100倍。

## 安装
``` pip install numpy ```

```python
import numpy as np
print(np.__version__) # 输出版本号就意味着安装成功
```
## 创建数组

### np.array()
>从列表或者元组创建数组

``` np.array(object, dtype=None, copy=True, order=None, subok=False, ndmin=0)```

- object：数组或嵌套列表
- dtype：数组元素的数据类型
- copy：对象是否需要复制
- order：创建数组的样式
- subok：默认返回一个与基本数据类型一致的数组
- ndmin：生成数组的最小维度

```python
import numpy as np

#创建一维数组（向量）
arr1 = np.array([1,2,3])
print(arr1) # [1,2,3]

#创建二维数组（矩阵）
arr2 = np.array([[1,2,3],[4,5,6]])
print(arr2) 
'''
[
    [1,2,3],
    [4,5,6]
]
'''

#创建出的数组是可以进行遍历的
for i in arr1:
    print(i, end=" ")

print()

for i in arr2:
    for j in i:
        print(j, end=" ")
```

### np.arange(start,end,step)

>类似range返回数组
```python
import numpy as np

arr3 = np.arange(1,11,2)
print(arr3) # [1 3 5 7 9]
```

### np.linspace(star,stop,num)

>用于创建一个一维等差数列的数组

```np.linspace(star,stop,num=50,endpoint=True,retstep=False,dtype=None)```

- start：序列起始值
- stop：序列的终止值如果endpoint为true该值包含于数列中
- num：要生成的等长的样本数量，默认50
- endpoint：该值为true时数列中包含stop的值反之则不包含默认为True
- dtype：数据类型

```python
arr3 = np.linspace(1,10, 5, dtype=int)
print(arr3) # [ 1  3  5  7 10]
```
### np.zeros(shape)

>创建指定大小的数组，数组元素以 0 进行填充
```python
arr3 = np.zeros(5)
print(arr3) #[0. 0. 0. 0. 0.]
```

### np.ones(shape)

>创建指定大小的数组，数组元素以 1 进行填充
```python
arr3 = np.zeros(5)
print(arr3) #[1. 1. 1. 1. 1.]
```

### np.full(shape,val)

> 填充指定值
```python
arr3 = np.full(5,3)
print(arr3) #[3 3 3 3 3]
```

### np.eye(n)

> n阶单位矩阵
```python
arr3 = np.eye(3,3)
print(arr3)
'''
[[1. 0. 0.]
 [0. 1. 0.]
 [0. 0. 1.]]
'''
```

### np.random.randn(shape)

>标准正态分布
```python
arr3 = np.random.randn(2,3)
print(arr3) # 均值0，方差1

'''
[[-1.23267799 -0.35877359  0.10939367]
 [ 1.05827112  1.51980385  1.0985282 ]]
'''
```

### np.random.rand(shape)

>0~1均匀分布
```python
arr3 = np.random.rand(2,3) #生成2*3 数组，元素在[0,1)之间均匀分布
print(arr3)

'''
[[0.39245459 0.82200044 0.32446012]
 [0.57663817 0.36884971 0.75662484]]
'''
```

### np.random.randint(low,hig,size)

>随机整数
```python
arr3 = np.random.randint(1,10,size=(3,4))
print(arr3)
'''
[[7 6 8 3]
 [9 7 3 2]
 [2 7 9 3]]
'''
```

## 广播机制
>广播机制是对不同形状的数组进行数值计算的方式，对数组的算术运算通常在相应的元素上进行

**两个数组的形状相同**

> 如果两个数组a和b形状相同，那么a*b的结果就是a与b数组对应位相乘

```python
a=np.array([1,2,3,4])
b=np.array([10,20,30,40])
c=a*b
print(c) # [ 10  40  90 160]
```

**两个数组的形状不同时，numpy将自动触发广播机制**
```python
import numpy as np
a = np.array([2,2,2])

'''
a会扩展为
[
    [2,2,2],
    [2,2,2],
    [2,2,2],
]
'''

b = np.array([
    [10,10,10],
    [20,20,20],
    [30,30,30]
])
c=a*b
print(c) 

'''
[[20 20 20]
 [40 40 40]
 [60 60 60]]
'''
```
### 广播的规则
1. 让所有输入数组都向其中形状最长的数组看齐，形状中不足的部分都通过在前面加1补齐。
    ```python
    '''
    在读这段话时不理解什么是在前面加1，其实加1就是在维度上加1。
    比如：
        a=[[1,2,3],[4,5,6]] 是一个二维数组 ->形状是(2,3)
        b=[1,1,1] 是一个一维数组 ->形状是(3,)
        b的维度不足在其左侧加1变为：
        [[1,1,1],[1,1,1]]
        因此a+b=[[2,3,4],[5,6,7]]
    '''
    import numpy as np
    a=np.array([[1,2,3],[4,5,6]])
    b=np.array([1,1,1])
    c=a+b
    print(c) # [[2,3,4],[5,6,7]]
    ```
2. 输出数组的形状是输入数组形状的各个维度上的最大值
    - 比如上述代码输出的维度就是2
3. 如果输入数组的某个维度和输出数组的对应维度的长度相同或者其长度为1时，这个数组能够用来计算，否则报错
4. 当输入数组的某个维度的长度为1时，沿着此维度运算时都用此维度上的第一组值
    - 数组拥有相同形状
    - 当前维度的值相等
    - 当前维度的值有一个是1

## 切片和索引
